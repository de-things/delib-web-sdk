`index.html` is the frontend showcase of how `delib.js` works. Example command calls are configured for the local `127.0.0.1:80` server, which you can run from the **[./server/](./server)** directory.
